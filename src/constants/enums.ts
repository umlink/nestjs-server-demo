export enum RolesEnums {
  User = 'USER',
  Vip = 'VIP',
  Admin = 'ADMIN',
  SuperAdmin = 'SUPER_ADMIN',
}

/**
 * 其它更细粒度的权限控制
 */
export enum PermissionEnums {
  Create = 'Create',
  Read = 'Read',
  Update = 'Update',
  Delete = 'Delete',
}

export enum PrismaEnum {
  NoData = 'P2025',
}
